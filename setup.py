from setuptools import setup, find_packages

setup(
    name="weather_api",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[],  # Add any dependencies here
    description="A package for retrieving weather data",
)